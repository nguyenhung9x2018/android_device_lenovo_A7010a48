To apply patches copy patch to working dir (e.g frameworks/base)
git status (check if they are already commited, if no then you will see your patch name with red color.)
git add . 
git commit -m "name of patch" 
All set ! Repeat this and install patches !
